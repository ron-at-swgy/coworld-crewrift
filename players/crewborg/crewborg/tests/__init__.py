"""Test package for the crewborg policy (``crewborg``).

Marks ``tests`` as an importable package so suites can share helpers via
``from crewborg.tests import sprite_wire`` (the synthetic Sprite-v1 message
builder used by the decoder/resolve tests).
"""
