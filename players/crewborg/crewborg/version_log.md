# Crewborg Version Log

This log maps uploaded `crewborg` policy versions to the code and runtime
configuration they carry. Older live versions existed before this repo-local log
was started; reconcile them with `policy_lifecycle.py versions --name crewborg`
if they become relevant.

| Version | Policy version ID | Uploaded at (UTC) | Source | Runtime config | Notes |
| --- | --- | --- | --- | --- | --- |
| v82 | `b70c0957-08b8-4d69-92e8-2766e8a850d7` | 2026-07-01T20:1xZ | same image as v81 (`players-crewborg:v81`, merged main `2d46468`) | `--run python -m crewborg.coworld.policy_player` + `--secret-env CREWBORG_METRICS=1 CREWBORG_TRACE_GROUPS=all` (per the new all-telemetry preference in `user_preferences.md`) | **v81 + full telemetry.** 8-ep probe `xreq_fc9863a0`: 0 timeouts with `all` tracing. NB policy artifacts only upload from LEAGUE (dispatch) episodes — 0/288 xreq eps ever had them — so telemetry lands once it plays rounds. **SUBMITTED to Crewrift Prime 2026-07-01 (`sub_cca840cf`) → QUALIFIED + 👑 CHAMPION** (`lpm_2449fb14`, competing/champion — supersedes v80). Known residual issues (from the v81 fingerprint warehouse `/tmp/v81_fp_wh`): (1) ~15% of crew seats freeze all game with 0 task attempts, 1 room entered — role-limbo (`self_role=None` when reveal text never parses; the v75 "unknown≠crew" stance has NO escape → violates the idle-escape principle); (2) **crew player-voting is DEAD** — 19/21 skips in v81, 0/440 player votes in v80's warehouse (June-30 baseline: ~51% vote rate) — broke somewhere in this lineage; the top crew lever to fix next. |
| v81 | `5d718fcc-9cad-4ac4-bd76-54566367cf96` | 2026-07-01T20:01Z | merged main `2d46468` (= v80's idle/search fixes + the v75 role-latch fix `4e1d7c1`); `build_player.sh crewborg --tag players-crewborg:v81` | `--run python -m crewborg.coworld.policy_player` (deterministic, std env — same as v80) | **v80 minus the role-latch regression.** Merge of `worktree-crewborg-idle-warehouse` (RECON de-freeze + SEARCH 5-state FSM + scored PICK_ROOM — the v77-v80 line) into main, which carries the IMPS-text latch fix v80 was missing. 484 tests pass; Gate-1 local run clean. Fingerprint check pre-submit: small natural-roles Prime xreq — expect 0-task crew ~0-10% (v80: 49%). |
| v80 | (see policy page) | 2026-07-01Z | worktree `crewborg-idle-warehouse`; `build_player.sh crewborg --tag players-crewborg:v80` | `--run …policy_player` (deterministic; PICK_ROOM weights env-tunable via `CREWBORG_PICKROOM_W_*`) | **v79 + RECON de-freeze + scored/tunable PICK_ROOM.** (1) **RECON never idles/stalls:** selector (`rule_based`) gates recon to the strictly PRE-ready window (`not self_kill_ready`), so a ready-but-blind imposter goes to SEARCH instead of beelining to a stale last-known position (the 9,289-tick freeze); `recon.py` abandons a reached-stale target and falls back to seeking expected crew (`best_seek_point`), never idle. (2) **PICK_ROOM is now a scored arg-max** over ALL reachable rooms (`modes/search.py:_room_score`), blending expected crew occupancy (strongest, via new `agent_tracking.room_occupancy`), unvisitedness (grows over time → peripheral coverage), a fast-decaying recency penalty (anti ping-pong), travel cost, teammate-pressure subtraction, task-room bonus, and a soft commander nudge — all weights env-tunable (`CREWBORG_PICKROOM_W_{OCCUPANCY,UNVISITED,RECENCY,DISTANCE,TEAMMATE,TASKBONUS,COMMANDER}`, decay/full-tick constants too). Hard commander hunt/avoid stay hard. Also the FOLLOW same-room→SEARCH_ROOM handoff. **482 crewborg tests pass, ruff clean.** Matched A/B: `xreq_2262f241` (v80) vs `xreq_fc3a3980` (v79), same fixed roster as the v76/v77 A/B. **RESULT — decisive (4-way, same roster): idle&ready 0.68 (v76) → 0.59 (v77) → 0.44 (v79) → 0.10 (v80); freezes≥1k 23 → 14 → 9 → 1; timeout-draws 0.38 → 0.28 → 0.20 → 0.07; kills 1.18 → 1.43 → 1.27 → 1.91 (v79→v80 t=3.95 p<0.001); imposter win 0.42 → 0.42 → 0.63 → 0.78.** The recon de-freeze was the big lever, as diagnosed. (v80 n=55: 5/60 eps were the known all-crew platform degenerate — excluded, not crashes.) **SUBMITTED to Crewrift PRIME 2026-07-01 (James's go-ahead)** — `sub_3fc853d2` → PLACED (`lpm_a95f8e29`), **competing + champion** (auto-champion=always; superseded v77). Post-submit tournament eval `xreq_c10927d1` (100 eps natural roles). **⚠️ CARRIES THE ROLE-LATCH REGRESSION:** built from a worktree forked before the v75 fix (`4e1d7c1`) — league survey: **49% of crew games end 0-task** (field 0-5%), telemetry shows crew latching imposter at RoleReveal. Superseded by v81 (this worktree merged into main = idle fixes + latch fix). |
| v79 | (see policy page) | 2026-07-01Z | worktree `crewborg-idle-warehouse`; `build_player.sh crewborg --tag players-crewborg:searchrework` | `--run …policy_player` (deterministic, std env) | **SEARCH FSM rework (James's spec; design = imposter-FSM doc §8).** Full rewrite of `modes/search.py` into a 5-state machine: **PICK_ROOM** never idles (fallback ladder: nearby task rooms → any task room → nearest room of any kind → seek hottest-occupancy cell); **GO_TO_ROOM** follows on seeing ANY visible crewmate (room OR hallway), arrives → **SEARCH_ROOM** (new: sweeps room interior scan-points to find crew hidden from the door); **WATCH** entered only with crew confirmed, branches **multi**→max-crew vantage vs **single**→close on target (near task-site, else kill_range+15px≈35px), self-loops only while ≥1 crew in view, last-leaves→FOLLOW, no-crew fallback→PICK_ROOM; **FOLLOW** keeps path-prediction persistence, on end re-scans room (→SEARCH_ROOM) or re-picks. Removes the v77 re-acquire hack + v78 timeout (both subsumed). **477 crewborg tests pass** (search suite rewritten: 22 pass); image verified (SEARCH_ROOM present). Eval: `xreq_fbfc2173` (v79 pinned imposter @slot0, 7 random Prime champions, 2 imposters, 100 eps). NOT submitted. |
| v78 | (see policy page) | 2026-07-01Z | worktree `crewborg-idle-warehouse`; `build_player.sh crewborg --tag players-crewborg:idlefix2` | `--run …policy_player` **+ `--secret-env CREWBORG_WATCH_IDLE_TIMEOUT=200`** (H1 always on + H2 timeout on) | **Idle-leak fix experiment arm 2 (H1+H2).** = v77's re-acquire fix PLUS an env-gated **WATCH idle-timeout**: after >200 consecutive idle ticks held at a vantage with no kill, force `SearchMode` back to `pick_room` (`modes/search.py`; `_watch_idle_ticks` counter, `CREWBORG_WATCH_IDLE_TIMEOUT`, default 0/off). Targets the residual multi-thousand-tick freezes (ereq_582 stood still 8,394 ticks → timeout draw). 103 search/mode tests pass. A/B arm `xreq_e99d7567` (crewborg pinned imposter slot0, matched vs v76/v77). NOT submitted. |
| v77 | (see policy page) | 2026-07-01Z | worktree `crewborg-idle-warehouse`; `build_player.sh crewborg --tag players-crewborg:idlefix` | `--run …policy_player` (deterministic, std env) | **Idle-leak fix experiment arm 1 (H1: re-acquire on ready).** Investigation of crewborg's imposter idle (warehouse over `xreq_3411a283`) found it spends **~26% of live imposter play idle while the kill is READY** (vs 1–3% for the aaln forks), because when the cooldown is up but no victim is in current line-of-sight the selector stays in Search→WATCH, which idles at a vantage ~267px from a crewmate instead of closing. Fix (`modes/search.py:_watch`): when `self_kill_ready` and a live non-teammate crewmate was seen recently, `navigate_to` its last-known position to force re-acquisition (Hunt then strikes) instead of idling. 105 search/mode tests pass; Gate-1 satisfied empirically (60/60 clean hosted A/B episodes, 0 failures). A/B `xreq_75af8d5d` (matched vs base v76 `xreq_da7a6fe0`): idle-while-ready 68%→59%, kills 1.18→1.43 (+21%, p≈0.08), 0-kill games 11→5, freezes≥1k 23→14, timeout-draws 38%→28%; win flat. **SUBMITTED to Crewrift Prime 2026-07-01 (James's go-ahead) as `sub_1ebf765e-458d-4d55-a235-94ec01e15633` (auto-champion=always) → QUALIFIED + 👑 CHAMPION** (membership `lpm_f7869302-3754-445d-af00-d067b9637259`, verdict `competing`, promoted over v70). A stopgap before the full SEARCH rework. |
| v75 | `e349f52d-1643-4ac8-abfe-87ab49597f16` | 2026-07-01Z | main @ role-latch fix (`4e1d7c1`) + 0x85 revert (`ab92f3c`) + dead-flag (`72a14a0`); `build_player.sh crewborg` | `--run python -m crewborg.coworld.policy_player` (deterministic, std env) | **Fixes the crew role-detection regression from `1178f31`.** That "robust teammate latch" (see `crewborg-paritypush:v1` below) widened the RoleReveal latch to fire on ANY `reveal_player_colors`, but the CREW reveal *also* renders player icons in the 9500+ range — so **every crew false-latched imposter and played the whole game as one (0 tasks)**. Verified 15/15 A/B crew seats latched imposter; task count was a death artifact (survive→0 tasks, die→ghost-task). Fix: role is latched positively from the RoleReveal **text** (`IMPS`→imposter+teammates, `CREWMATE`→crewmate), re-checked every reveal tick; `self_role=None` until seen (unknown≠crew); HUD no longer infers role (kill-icon→kill_ready only). Also: `dead` split from role into a `self_alive` flag; **0x85 per-tick send reverted** (A/B: net-harmful, an aggravator of the collapse). 476 tests. Measuring crew recovery: `xreq_300b95e7`. NOT submitted. |
| v73 | (see policy page) | 2026-06-29Z | nightly_refit.sh (automated) | std env | **Nightly suspicion refit** (nightly-2026-06-29): same code, re-fitted weights on the full corpus (264118 games, CV AUC 0.8109689629932723). Gates passed: AUC/corpus floors, test suite, Gate-1 smoke. Auto-submitted to the Crewrift league per James's standing instruction (2026-06-12). |
| **`crewborg-paritypush:v1`** (A/B candidate — SEPARATE policy name) | `db8d8ee2-2a27-46ae-bef0-b49c2a7f0453` | 2026-06-30T19:17Z | worktree `worktree-imposter-kill-to-win` @ `1178f31`; `build_player.sh crewborg --tag players-crewborg:parity-push` | `--run python -m crewborg.coworld.policy_player` (deterministic, NO Bedrock — matches the deterministic sweep) | **Imposter kill→WIN: close parity in the meeting + robust teammate latch.** (1) `parity_closing_vote_target`: one removal from parity AND known live teammate ⇒ manufacture a coordinated fabricated-accusation+vote on a non-teammate crewmate instead of skipping (self-gated on `alive_imposter_count>=2`; only at gap==1). (2) Widened RoleReveal teammate latch (types.py) — latch `reveal_player_colors` on sight. Diagnosis: 64% of crewborg imposter losses = an imposter voted out; rest stall at 3-crew/2-imp; notsus closes via meeting (1.10 crew-eject/win vs ~0.4). 470 tests pass. **Uploaded under a UNIQUE name** (`crewborg-paritypush`, not `crewborg`) to dodge parallel-session clobber of the shared `crewborg`/`players-crewborg:dev`. A/B: 6 pinned-champion 1v1 blocks (both imposters = subject, 6-of-each-champion crew, 20 eps) vs `crewborg-base`. |
| v72 | (see policy page) | 2026-06-27Z | nightly_refit.sh (automated) | std env | **Nightly suspicion refit** (nightly-2026-06-27): same code, re-fitted weights on the full corpus (264118 games, CV AUC 0.8109656257139269). Gates passed: AUC/corpus floors, test suite, Gate-1 smoke. Auto-submitted to the Crewrift league per James's standing instruction (2026-06-12). |
| v70 ⭐⭐ CHAMPION (Crewrift PRIME) | (see policy page) | 2026-06-26Z | merged main (v69 imposter code: witness-drop + 72t Evade re-approach, EVADE_TICKS=72; **+ LLM commander code merged but gated OFF**; coworld-tools SDK); `tools/build_player.sh crewborg --tag players-crewborg:ship-llm2` | **`--use-bedrock --bedrock-model us.anthropic.claude-haiku-4-5-20251001-v1:0` + `--secret-env CREWBORG_LLM_MEETINGS=1 CREWBORG_CHAT_NLP=1 CREWBORG_METRICS=1` + trace `voting,action,decision,llm,meeting`** — **meeting LLM ON, commander OFF, NO manual USE_BEDROCK** (sidecar-endpoint gating, the correct way) | **THE LLM-ON SHIP — now PRIME CHAMPION.** = v69's confirmed deterministic imposter combo (witness-drop the significant driver: v63 vs v54 +19pp ≥2-kill / +14pp win / +0.32 kills p=0.038) + the **meeting LLM** turned on, on the merged commander codebase (commander OFF by env). **Meeting LLM VERIFIED firing** pre-submit: 8-ep probe `xreq_bc2878d1`, crewborg's own artifact = **22 `meeting_llm_decision`, 0 fallback**. 461 tests, Gate-1 PASS. **SUBMITTED to Crewrift PRIME 2026-06-26 (James's go-ahead) → PLACED into Prime Competition + CHAMPION** (`sub_243b5455`, `lpm_60b71147`, champion=True — supersedes v69). ⚠️ OPEN: whether the meeting LLM fires in LEAGUE (dispatch) rounds vs only xreq (k8s) — v66 fell back in league; verify once v70's league rounds run. |
| v71 | (see policy page) | 2026-06-26Z | nightly_refit.sh (automated) | std env | **Nightly suspicion refit** (nightly-2026-06-26): same code, re-fitted weights on the full corpus (264118 games, CV AUC 0.8109669200759004). Gates passed: AUC/corpus floors, test suite, Gate-1 smoke. Auto-submitted to the Crewrift league per James's standing instruction (2026-06-12). |
| v69 ⭐⭐ CHAMPION (Crewrift PRIME) | `41b61a77-78b7-4e3b-8c49-1cced8aa04e2` | 2026-06-26Z | thin image `FROM players-crewborg:ship-v63llm` + `ENV CREWBORG_LLM_MEETINGS=0 CREWBORG_LLM_COMMANDER=0` (same confirmed v63 imposter code, **LLM OFF**) | `--run …policy_player` + `--secret-env CREWBORG_METRICS=1 CREWBORG_TRACE_GROUPS=voting,action,decision CREWBORG_TRACE_DECISION_FIELDS=… CREWBORG_CHAT_NLP=1` — **deterministic, NO --use-bedrock / no LLM** (the LLM only fires in k8s/xreq pods, never league/dispatch rounds, so an LLM-on submission just adds startup weight + silent fallback) | **THE DETERMINISTIC SHIP — now PRIME CHAMPION.** Same play code as v66 (witness-drop after 1st kill = the significant driver: v63 vs v54 natural roles +19pp ≥2-kill / +14pp win / +0.32 kills p=0.038; + inert 72t Evade re-approach; EVADE_TICKS=72), LLM stripped. 425 tests, Gate-1 PASS. **SUBMITTED to Crewrift PRIME 2026-06-26 (James's go-ahead).** First two tries (`sub_3fb0aaac`) REJECTED ("league has no submission division" — Prime missing its Qualifiers staging division / stale `qualifiers_division_name` config). After a **backend fix** (James), re-submit `sub_ad7dba1e` **PLACED into Prime Competition (div_acbde92a) and is now CHAMPION** (`lpm_3bc70241`, champion=True, auto_champion=always) — **supersedes v42.** |
| v66 ⭐ SHIPPED (Crewrift PRIME) | `d7f5dbbb-e557-4efe-9ae3-8d0d08f6cb3b` | 2026-06-26Z | merged main (LLM commander code) + the **confirmed v63 imposter combo** (Evade re-approach 72t + witness-drop after 1st kill, EVADE_TICKS=72); `tools/build_player.sh crewborg --tag players-crewborg:ship-v63llm` | **`--use-bedrock --bedrock-model us.anthropic.claude-haiku-4-5-20251001-v1:0` + `--secret-env CREWBORG_LLM_MEETINGS=1 CREWBORG_CHAT_NLP=1 CREWBORG_METRICS=1` + trace groups `voting,action,decision,llm,meeting`** — **meeting LLM ON, commander OFF, NO manual USE_BEDROCK** (gates on the sidecar endpoint, the correct post-fix way) | **THE SHIP: confirmed imposter 2nd-kill combo + meeting LLM.** Carries the witness-drop-after-1st-kill (the significant driver: v63 vs v54 +19pp ≥2-kill / +14pp win / +0.32 kills p=0.038, natural roles) + the inert-but-harmless 72t re-approach, on top of the merged LLM-commander codebase (commander gated OFF). **Meeting LLM VERIFIED firing** before submit: 8-ep probe `xreq_2e49c8f9`, crewborg's own artifact = **17 `meeting_llm_decision`, 0 fallback**. 425 tests, ruff-clean, Gate-1 PASS. **SUBMITTED to Crewrift PRIME 2026-06-26 (`sub_9851f507`, James's go-ahead) → REJECTED: `notes="league has no submission division"`** — a PLATFORM/league-config issue (Prime migrated 2026-06-24 to among-them-commissioner + Qualifiers flow; direct `--league` submit broken; same as v49), NOT our play (placement games mean +8, imposter 1.33 kills). **ALSO: meeting LLM FELL BACK in league rounds** (0 `meeting_llm_decision` vs 17 in the xreq probe) — Bedrock sidecar is wired for experience-requests, NOT league pods, so the LLM plays deterministically in league. Confirmed shippable gain = the deterministic witness-drop. |
| v65 (A/B candidate) | (see policy page) | 2026-06-26Z | clean HEAD + Evade re-approach + witness-drop + **SUSTAINED re-approach (EVADE_TICKS 72→400)**; `tools/build_player.sh crewborg --tag players-crewborg:cand-combo2` | `--run …policy_player` (`CREWBORG_EVADE_TICKS` env-tunable, default 400) | **Sustained-re-approach combo (James 2026-06-26).** v63's combo HELPED imposter (≥2-kill 35→50%, kills +0.26 p=0.056) but the mechanism check showed the **re-approach was inert** (72t washed out by the 500t-later ready; post-kill in-view 40→42% unchanged) — the gain was the witness-drop. James: "move to where we think people are after a kill" should help, so **extend the post-kill occupancy re-approach window 72→400t** so we're actually with the crew crowd at the 2nd ready (where witness-drop converts it). 377 tests, ruff-clean, Gate-1 PASS. **3-arm A/B NATURAL roles (300/arm). VERDICT: sustaining HURT.** Fully-clean imposter: v65 ≥2-kill 46%/win 70%; v63 54%/88%; v54 35%/73%. **v63 vs v54 = +19pp ≥2-kill, +14pp win, +0.32 kills p=0.038 (SIGNIFICANT — combo confirmed).** v65 vs v63 = −8pp ≥2-kill, **win 88→70%** (camping the crowd 400t reads suspicious → ejected more, no kill gain). EVADE_TICKS reverted to 72; witness-drop is the driver, 72t re-approach inert-but-harmless. **Best tested = v63.** NOT submitted. |
| v64 | (see policy page) | 2026-06-26Z | worktree `labs-work` @ (meeting endpoint-gating) | `--use-bedrock --secret-env USE_BEDROCK=true CREWBORG_LLM_COMMANDER=1 CREWBORG_LLM_MEETINGS=1 CREWBORG_CHAT_NLP=1` + `CREWBORG_TRACE_GROUPS=commander,llm,meeting,voting` | **✅ BOTH LLMs CONFIRMED FIRING IN-POD.** Applied the same sidecar-endpoint gating to the **meeting LLM** (`strategy/meeting/llm.py` now ORs `AWS_ENDPOINT_URL_BEDROCK_RUNTIME` into the backend check), reviving it. In-pod (XP `xreq_731b5f8f`, 6-ep Crewrift Prime): **meeting LLM = 290 `meeting_llm_decision`, ZERO `meeting_llm_fallback`** (was 184/184 fallback); **commander = 4637 `commander_call {outcome:ok}`, 0 errors**, `enabled:true backend:bedrock endpoint_seen:true`. So both the champion's meeting chat and the new gameplay commander now run on Bedrock via the sidecar with no platform change. 424 tests green. **Not submitted.** |
| v63 (A/B candidate) | (see policy page) | 2026-06-26Z | clean HEAD + **BOTH** Evade re-approach (v53) **+** witness-drop-after-1st-kill (v58); `tools/build_player.sh crewborg --tag players-crewborg:cand-combo` | `--run …policy_player` (lean) | **COMBINED candidate (James's direction 2026-06-26): beeline the densest crowd AND ignore witnesses after the 1st kill.** Synergy hypothesis: v53 alone (re-approach the crowd) failed because the crowd is witness-rich and Hunt's gate blocked the 2nd kill; v58 alone is a narrow lever; TOGETHER, re-approaching a target-rich crowd + not caring about witnesses should convert the 2nd kill. Witness gate at Hunt's strike site (clean, not the dishonest `unwitnessed` override). 377 tests, ruff-clean, Gate-1 PASS. **A/B vs v54 in NATURAL ROLES** (the config the gap actually shows: v54 baseline ≥2-kill 52% vs Aaron/Andre 82%) — 300 eps/arm, cand `xreq_31967adb/059cb124/6df87688`, base(v54) `xreq_50173800/4f2a3ca7/62a66659`, interleaved same window. NOT submitted. |
| v62 | (see policy page) | 2026-06-26Z | worktree `labs-work` @ `d43149d` (sidecar-endpoint gating) | `--use-bedrock --secret-env USE_BEDROCK=true CREWBORG_LLM_COMMANDER=1` + `CREWBORG_TRACE_GROUPS=…,commander`, `CREWBORG_LLM_TRACE_RAW=1` | **✅ COMMANDER CONFIRMED FIRING IN-POD.** Fix: the commander now treats `AWS_ENDPOINT_URL_BEDROCK_RUNTIME` (the sidecar endpoint) as a Bedrock signal instead of relying on `USE_BEDROCK` — sidecar mode strips `USE_BEDROCK` and injects the endpoint + dummy creds (infra agent confirmed: `kubernetes_runner.py` strips direct Bedrock env). In-pod (XP `xreq_5a87445f` on Crewrift Prime): `commander_started {enabled:true, backend:bedrock, env_seen:{USE_BEDROCK:false, CLAUDE_CODE_USE_BEDROCK:false, AWS_ENDPOINT_URL_BEDROCK_RUNTIME:TRUE}}` and **672 `commander_call {outcome:ok}` (0 errors, ~1.8s latency)**. So the sidecar IS deployed for crewrift_prime XP jobs (it injects the endpoint); only `USE_BEDROCK` was missing, which the gating fix sidesteps. Modes still don't read priorities (Phase 2), so no gameplay change yet — but the LLM pipeline is now live end-to-end in-pod. **NB the meeting LLM still gates on `USE_BEDROCK` (SDK `bedrock_enabled`) → still disabled in-pod; same one-line fix would revive it.** Phase 2 now unblocked. **Not submitted.** |
| v59 | (see policy page) | 2026-06-26Z | worktree `labs-work` @ `52398eb` (lazy client + env diagnostic) | `--use-bedrock --secret-env USE_BEDROCK=true CREWBORG_LLM_COMMANDER=1` + `CREWBORG_TRACE_GROUPS=…,commander`, `CREWBORG_LLM_TRACE_RAW=1` | **ROOT CAUSE PROVEN = infra, not code.** Commander client now built LAZILY in the worker thread from live `os.environ` (mirrors the meeting LLM) with a 20×/0.5s retry on "no LLM backend", plus an `env_seen` diagnostic on `commander_started`. Local Gate-1: `enabled:true, attempt:1, env_seen:{USE_BEDROCK:true}` — fires. **In-pod (XP `xreq_…` on Crewrift Prime, clean real game, 0 timeouts): `commander_started {enabled:false, disabled_reason:"no LLM backend configured", env_seen:{USE_BEDROCK:false, CLAUDE_CODE_USE_BEDROCK:false, ANTHROPIC_API_KEY:false}}` on all 8 slots** — i.e. NONE of the Bedrock env vars are truthy in the player container, at startup or after retries. So lazy-vs-eager timing was NOT the cause; the Bedrock backend env simply isn't present in crewrift_prime XP-request player containers. Note `CREWBORG_LLM_COMMANDER` (same secret-env mechanism) DOES reach the container → `USE_BEDROCK` is being consumed/stripped by the platform to gate sidecar attachment, and the sidecar isn't attached for these jobs (v50-saga; the crewrift_prime XP Bedrock sidecar was a manual enablement, "TF reconciliation owed"). **Strong inference: the meeting LLM would ALSO be disabled in these pods right now** (same `bedrock_enabled` check) — the v50 "working" state may have reverted. **INFRA ACTION (James): (re-)enable + persist the Bedrock sidecar for crewrift_prime experience-request jobs.** Commander code is correct (proven locally). **Not submitted.** |
| v58 (A/B candidate) | (see policy page) | 2026-06-26Z | clean HEAD + **witness-gate change** (`tools/build_player.sh crewborg --tag players-crewborg:cand-witness`); Evade re-approach REVERTED so this is isolated | `--run python -m …policy_player` (deterministic) | **Drop the unwitnessed requirement after our FIRST kill (James's direction 2026-06-26).** `strategy/opportunity.py` `unwitnessed()` returns `True` once `belief.last_kill_tick is not None` → Hunt strikes a witnessed victim once a kill is banked. Rationale: getting the **2nd** kill is the imposter's core job (2 imp × 2 = 4 = parity); at the 2nd ready we're usually already close (meeting-aware @ready ~49px, in-view ~57%) so CONVERSION, not stealth, is the bottleneck. NB the v50 "drift" framing was partly a meeting-contaminated distance curve (see tentative lessons) — the real lever is conversion. 377 tests (+1), ruff-clean, Gate-1 PASS. **A/B vs v54 baseline:** fixed-Andre `xreq_897c3f9f…`(base)/`xreq_df8e65df…`(cand); RR co-imp `xreq_d3dc4672…`(base)/`xreq_b359f6e6…`(cand) — 100 eps each, 2-imp imposter-pinned. **VERDICT: NEUTRAL** (fully-clean: kills 1.82→1.77 / 1.64→1.68, ≥2-kill 69→69% / 64→61%, win +7/+3pp noise). Why: the witness gate already self-relaxes via urgency to 0 by ~10s, so dropping it after the 1st kill changes only a narrow window. **KEY: the baseline v54 already gets ≥2-kill 69% / win 75-82% — the v50 "44% 2nd-kill / drift" diagnosis is STALE for current code.** Refactored to gate at Hunt's strike site (not a dishonest `unwitnessed` override) — behavior-identical, so this A/B stays valid; v58 image carries the pre-refactor impl (rebuild from clean tree if ever shipping). NOT submitted. |
| v57 | (see policy page) | 2026-06-26Z | worktree `labs-work` @ `b61fa20` (commander + observability); image `tools/build_player.sh crewborg` | **`--use-bedrock --secret-env USE_BEDROCK=true CREWBORG_LLM_COMMANDER=1`** + `CREWBORG_TRACE_GROUPS=decision,action,voting,commander`, `CREWBORG_LLM_TRACE_RAW=1`, `CREWBORG_METRICS=1` | In-pod confirmation attempt with the commander trace group ON. XP `xreq_8430eaca` on Crewrift Prime completed clean (real game, 0 timeouts). **FINDING (via the new `domain.commander_started` trace): commander is DISABLED in-pod — `{enabled:false, disabled_reason:"no LLM backend configured"}` on all 8 slots**, i.e. `bedrock_enabled(env)` is False (`USE_BEDROCK`/`CLAUDE_CODE_USE_BEDROCK` not truthy in the player container's `os.environ`) even though `--use-bedrock` sets `USE_BEDROCK=true` (CLI `cli.py:652`) and `CREWBORG_LLM_COMMANDER` IS seen. Not a 403, not a crash — the client is never constructed. Same in-pod Bedrock-enablement boundary the meeting LLM hit (the v50 saga; infra, not code). Candidate causes: (a) the commander builds its client EAGERLY at `build_runtime` startup, possibly before the pod's Bedrock env is populated — the meeting client builds LAZILY at meeting time and works; (b) the sidecar/bedrock env genuinely isn't present in these XP pods. **NEXT diagnostic:** add env-presence fields to `commander_started` (+ make client construction lazy like meetings), then one more in-pod run. **Not submitted.** |
| v56 | (see policy page) | 2026-06-26Z | same image as v57 (`b61fa20`) | `--use-bedrock --secret-env CREWBORG_LLM_COMMANDER=1` (NO explicit `USE_BEDROCK`) + `CREWBORG_TRACE_GROUPS=…,commander`, `CREWBORG_LLM_TRACE_RAW=1` | First commander build WITH observability. XP `xreq_8a23bf91` (Prime) clean. Showed the same `commander_started {enabled:false}` in-pod → ruled out "missing trace group" and surfaced the real disable. (v55's empty in-pod telemetry was BOTH a trace-group filter AND this disable.) |
| v55 | `1af90153-0e19-49fb-954d-c641955ab3f6` | 2026-06-26Z | worktree `labs-work` (branch `worktree-labs-work`) @ `92e66db`; image via `tools/build_player.sh crewborg` | **`--use-bedrock --secret-env CREWBORG_LLM_COMMANDER=1`** + `CREWBORG_METRICS=1`, `CREWBORG_TRACE_GROUPS=decision,action,voting`, `CREWBORG_LLM_TIMEOUT_SECONDS=5` | **FIRST LLM-gameplay-commander version (Phase 1: scaffold + wiring).** Design: `docs/commander.md` (design.md §10.6). A background daemon LLM writes sticky priorities into `belief.commander` via `StrategyResult.inferences` → `apply_inferences`; `CommanderStrategy` wraps `RuleBasedStrategy` on a `CloseAwareSynchronousStrategyRunner`; off → exactly current crewborg. **Modes do NOT yet read priorities (Phase 2+)**, so this carries NO gameplay change beyond the meeting LLM being OFF here — it is a pipeline/crash test. Built by Codex (codex-task), reviewed; 403 crewborg tests green, disabled-path proven byte-identical, ruff clean on commander code. **Gate-1 (local, flag ON + real Bedrock via temp softmax SSO creds): PASS** — clean Lobby→Playing→GameOver across all 8 self-play slots, 0 connect/disconnect timeouts, **commander fired (231–254 `strategy_inferences`/slot, priorities applied to belief every Playing tick), 0 error signatures** (no AccessDenied/botocore/traceback). Note the cert fixture's Playing phase DOES exercise the commander (unlike v47's meeting LLM). **Hosted crash test PASS:** 1-episode self-play XP request `xreq_6d62ac18-4523-4044-aec2-085e444ba993` on Crewrift Prime (cow `5e21fb01`, sidecar-Bedrock) completed 1/1, **0 failed** — a real full game (crew 108×6 with 8/8 tasks, imposters 10×2 with kills, votes cast, **0 connect/disconnect timeouts**), no crash, no error signatures. **BUT in-pod commander firing is UNCONFIRMED:** 0 `strategy_inferences` in the episode telemetry — confounded by (a) the in-pod trace groups (`decision,action,voting`) not including the SDK `strategy_inferences` event, and (b) the Phase-1 worker swallowing Bedrock exceptions silently (no trace/metric), so a silent in-pod Bedrock 403 (the v50-saga issue) would look identical. **Phase-1 observability gap → first Phase-2 task: add a `commander_decision`/`commander_error` trace+metric so firing/latency/fallback are visible in-pod.** **NOT submitted to any league.** Next: Phase 2 (imposter levers `hunt_room`/`target_player`/`avoid_room` + danger mode) so the priorities actually change behavior, then A/B vs the deterministic champion. |
| v54 (A/B baseline) | `1deb9f21-d7c2-468b-856a-edabc796cc5b` | 2026-06-26Z | **same tree MINUS the Evade rewrite** (git-stashed); `tools/build_player.sh crewborg --tag players-crewborg:base-noevade` | identical to v53 (`--run` only) | **A/B baseline = current code with the OLD flee-Evade** (vent away / move away from the body), isolating the Evade change as the only diff vs v53. Same weights/SDK/game; only `modes/evade.py` differs. NOT submitted; exists only for the v53 A/B. |
| v53 (A/B candidate) | `68e0f383-bbd4-4178-9e49-549d9b769859` | 2026-06-26Z | working tree (v50 lineage) + **Evade rewrite**; `tools/build_player.sh crewborg --tag players-crewborg:cand-evade` | `--run python -m crewborg.coworld.policy_player` (no extra env; deterministic, no Bedrock — controlled A/B) | **Evade = post-kill RE-APPROACH (James's direction 2026-06-26).** `modes/evade.py` rewritten: instead of vent-away / walk-off-the-body it **beelines toward the most-populated area** off the expected-crew occupancy grid (`agent_tracking.densest_crew_room_target` → `best_seek_point` → last-seen crew → idle). Targets the localized post-kill drift (crew-in-view at next ready ~29% vs Aaron ~80%; we drift ~110-200px and stay flat while Aaron closes to ~30-50px). Renamed `best_pretend_room_target`→`densest_crew_room_target` (Evade is now its real caller; Pretend retired). 376 tests pass, changed code ruff-clean, Gate-1 smoke PASS. **A/B vs v54:** candidate `xreq_72de410c-9ed4-408c-8005-028fab26331c` / baseline `xreq_978d5501-6689-4ea9-8b4d-2f1082b3284c` — 100 eps each, matched pinned roster (crewborg imp@slot0, Andre co-imp, 3 Aaron+3 Andre crew). **VERDICT: NEUTRAL** (fully-clean eps: kills 1.73→1.74; no-kill/≥2-kill identical). Mechanism: beelining to the densest CROWD ≠ killable isolated victim (v46 dead-end re-confirmed); also Evade is only 72t of the 500t cooldown. Change REVERTED from the tree (superseded by the v58 witness-gate direction). NOT submitted. |
| v52 | (see policy page) | 2026-06-25Z | nightly_refit.sh (automated) | std env | **Nightly suspicion refit** (nightly-2026-06-25): same code, re-fitted weights on the full corpus (264118 games, CV AUC 0.8109669933407769). Gates passed: AUC/corpus floors, test suite, Gate-1 smoke. Auto-submitted to the Crewrift league per James's standing instruction (2026-06-12). |
| v51 | (see policy page) | 2026-06-24Z | nightly_refit.sh (automated) | std env | **Nightly suspicion refit** (nightly-2026-06-24): same code, re-fitted weights on the full corpus (249260 games, CV AUC 0.8102006837386458). Gates passed: AUC/corpus floors, test suite, Gate-1 smoke. Auto-submitted to the Crewrift league per James's standing instruction (2026-06-12). |
| v47 | `91616701-5528-44cc-beb6-3ae972b71597` | 2026-06-25T22:41Z | worktree-main-20260625 (uncommitted); image via `tools/build_player.sh crewborg` (SDK `fdf2987`, game 0.1.59 manifest for Gate-1) | **`--use-bedrock --bedrock-model us.anthropic.claude-haiku-4-5-20251001-v1:0`** + `--secret-env CREWBORG_LLM_MEETINGS=1`, `CREWBORG_LLM_TRACE_RAW=1` (first-version debug), `CREWBORG_METRICS=1`, `CREWBORG_TRACE_GROUPS=voting,action,decision`, `CREWBORG_TRACE_DECISION_FIELDS=phase,role,mode,intent,command,voting,self`, `CREWBORG_CHAT_NLP=1` | **FIRST LLM-meetings-enabled version (the dormant seam lit up).** Design: `docs/meetings.md`. Adds a **Bedrock backend** to `strategy/meeting/llm.py` via the SDK helpers (`select_client`/`bedrock_enabled`/`resolve_model`/`call_json`/`extract_json_object`); enable = `CREWBORG_LLM_MEETINGS=1` AND (`USE_BEDROCK` or `ANTHROPIC_API_KEY`); factory wraps all SDK import/select in try/except → `DisabledMeetingClient` (never raises → deterministic fallback). **Per-role prompts** as external `strategy/meeting/memory/{crewmate,imposter}.md` loaded from `context["self"]["role"]` (`prompts.py`; `CREWBORG_LLM_PROMPT_DIR` override) — crew doctrine = vote *restraint* (preserve v25), imposter doctrine = conversion (deflect/bandwagon, never out a teammate). **Full LLM vote authority** (no confidence downgrade; legal-target + hard self-vote guard kept). **Timeout-derived latency guard** in `attend_meeting.py`: deadline trigger wins over late chat, and no LLM call starts unless a worst-case-timeout (`CREWBORG_LLM_TIMEOUT_SECONDS=3.0`) call can return before the 48-tick auto-submit (24 ticks/s, 240-tick=10s timer). Implemented by Codex (independent review + surgical impl), human-reviewed. **367 tests pass** (new `test_meeting_llm.py` + meeting-mode additions), ruff clean. **Gate-1: liveness PASS** under `--use-bedrock`+`CREWBORG_LLM_MEETINGS=1` (clean Lobby→Playing→GameOver, artifacts written, 0 errors) — but the **degenerate cert fixture has NO Voting phase, so the LLM meeting call was NOT exercised locally**; hosted Bedrock-haiku is already verified working for other players in this account. **NOT submitted to any league.** Next: experience-request eval (role-decomposed A/B vs the deterministic champion) to confirm the LLM actually fires (`meeting_llm_decision` events, latency, zero fallbacks) and doesn't regress crew vote-restraint. NOTE: nightly auto-refit uploaded v32–v46 (undocumented here); reconcile via `policy_lifecycle.py versions --name crewborg` if relevant. |
| v42 ⭐ SHIPPED (Crewrift PRIME) | (policy `75fb5760-c0c3-4d77-a19c-a754d06abb97`) | 2026-06-25Z | `players-crewborg:dev` via `tools/build_player.sh crewborg` (vantage-SEARCH code) | std env (`CREWBORG_TRACE_GROUPS=voting,action,decision`, decision fields, CHAT_NLP, METRICS; NO debug) | **New imposter SEARCH + vantage-watching.** Whole imposter rebuild: PRETEND removed; SEARCH is the always-on seeking stance (pick random nearby room → enter to centre → WATCH from the in-room VANTAGE with line-of-sight to the most crew, recomputed as they move → FOLLOW the first leaver via `strategy.path_prediction`). Built to fix the diagnosed gap (near crew ~half as often as top imposters); vantage-watching specifically (James caught in replays: imposter was walking out of crew's sight standing at the door). Also carries the env-tunable vote gate (default 0.9) + reconnect from v38/v40. **Confirmation eval (200 imposter eps vs Aaron+Andre LATEST — truecrew:v25, crewborg-aaln:v17, round-robin; n=180 clean):** kills/g **1.23±0.10** (v40 door 0.60, v38 0.85 → ~2×), 0-kill 11% (was 60%), ≥2k 30%. Imposter **win 34%** — DOWN vs v38's 43% despite more kills (harder field + kill→win conversion now the bottleneck; kills in wins 1.33 ≈ losses 1.20). NB ejection rate UNKNOWN (no score signal — must read logs). 357 tests, Gate-1 PASS (vs cached 0.1.54 manifest). **Submitted to Crewrift PRIME league 2026-06-25 (`sub_d1a32841-5149-4342-8a48-7e8a87711e27`, James's go-ahead).** v41 = same code + debug trace (eval-only, not shipped). |
| v38 (prior baseline) | `76be842b-6963-4728-9cad-8419a711fde1` | 2026-06-24Z | thin image `FROM players-crewborg:dev` (rebuilt w/ reconnect) + `ENV CREWBORG_WEIGHTS_VOTE_P=0.8` | std env + baked `CREWBORG_WEIGHTS_VOTE_P=0.8` | **v34 (gate 0.8) + aggressive initial-connect reconnect.** Supersedes v34 as the working baseline. Adds the bridge reconnect fix (`coworld/policy_player.py`): retries the initial connect on a flat 0.1s interval (env `CREWBORG_RECONNECT_INTERVAL`) up to a 120s deadline (`CREWBORG_RECONNECT_DEADLINE`), to kill the ~half of −100 connect_timeouts that were our own initial-connect races (verified: 0-frame, no-stderr, never-running episodes). 358 tests, Gate-1 PASS, bake verified (gate 0.8 + reconnect present). Eval vs Aaron+Andre (598 eps, 300 crew + 280 imp clean, rotating; crew xreqs `6bfb0d5d/652c79e0/dbd022d2`, imp `6f45e3b4/3089a859/a24ee0e2`): **ops-failure rate 65%→3.0%** (18/598) — the reconnect fix worked, evals are trustworthy again. **crew 52.3% CI[47,58], imposter 43.2% CI[38,49]** (vs v33's 36.5% imp in sweep2 — imp up sharply). v38 overall 47.9%, mid-pack with the field (Andre von Houck 54%, Andre Jr 49%, Aaron 48%, Aaron's Optimizer 46%). NOT submitted. |
| v34 (superseded by v38) | `96d20cc1-041f-4702-a1f4-6b273bc0fb37` | 2026-06-24Z | thin image `FROM players-crewborg:dev` + `ENV CREWBORG_WEIGHTS_VOTE_P=0.8` | std env + baked `CREWBORG_WEIGHTS_VOTE_P=0.8` | **The new working baseline (James, 2026-06-24): vote gate 0.9→0.8.** Chosen from the threshold sweep below — v33 (old 0.9 restraint gate) was the WORST arm in both sweeps (sweep1 vs top-7: 31.9%, lowest; sweep2 vs Aaron+Andre: 50.0%, lowest), and the lowered-gate arms v34–v37 cluster tightly together (sweep2 crew win 52–55%, overlapping CIs) ~4pp above v33. v34 (0.8) = the most conservative winning arm (smallest move off 0.9; top win% 55.3% in sweep2). v33 scrapped. ⚠️ the v33-vs-rest gap was underpowered in sweep2 (p=0.39) because a ~65% platform connect-timeout rate gutted clean-game counts (n=63–94/arm of 300) — but sweep1 confirmed 0.9 worst at p=0.031, so the direction is solid. NOT yet submitted. |
| v33, v35–v37 | v33 `bc90c3d4` / v35 `0f8a5df9` / v36 `414acef1` / v37 `a341b7bf` | 2026-06-24Z | thin images `FROM players-crewborg:dev` + `ENV CREWBORG_WEIGHTS_VOTE_P=<p>` | std env + baked `CREWBORG_WEIGHTS_VOTE_P` | **Crew vote-threshold sweep arms** (NOT submitted — eval only). Same code/weights as v32; gates: v33=0.9 (=v31/v32 behavior, SCRAPPED), v35=0.7, v36=0.6, v37=0.5. v34 (0.8) graduated to baseline (row above). Motivation: 2026-06-23 standing eval found v31 crew wins only 38% despite 8/8 tasks — loses the parity race by sitting passive at the 0.9 gate (votes 1.11/g in wins vs 0.23 in losses). Sweep xreqs in WORKING_CONTEXT. NB platform dedups by image digest, so distinct ENV-baked images were required. |
| v32 | `b48e7895-5d90-4564-89ff-e718e4494674` | 2026-06-24Z | main @ env-tunable-vote-threshold commit; `tools/build_player.sh crewborg` | std env (default `CREWBORG_WEIGHTS_VOTE_P=0.9`) | Same weights/behavior as v31; adds the `CREWBORG_WEIGHTS_VOTE_P` env knob (defaults to 0.9 = unchanged). First upload of the env-tunable build. NOT submitted. (5 `--secret-env` uploads of this image deduped into this one version — see v33–v37 for the actual sweep arms.) |
| v31 | (see policy page) | 2026-06-14Z | nightly_refit.sh (automated) | std env | **Nightly suspicion refit** (nightly-2026-06-14): same code, re-fitted weights on the full corpus (35520 games, CV AUC 0.8118094746112113). Gates passed: AUC/corpus floors, test suite, Gate-1 smoke. Auto-submitted to the Crewrift league per James's standing instruction (2026-06-12). |
| v25 | `36204c43-fc78-43b7-9d0f-23fe753e16e7` | 2026-06-12Z | main @ `7594e3c`; image via `tools/build_player.sh crewborg` | v24 env (`CREWBORG_METRICS=1`, trace groups/fields, `CREWBORG_CHAT_NLP=1`) | **The fitted suspicion model** (James's direction; design `suspicion_lab/README.md`). suspicion.py loads vendored `data/suspicion_weights.json` (v3-runtime: L1 logistic regression on 1,857 league games / 196k observer-suspect rows, CV AUC 0.812 = the full-feature ceiling): instance-summed evidence with per-context dedup, EXCULPATORY negative weights, exposure feature, witnessed kill/vent kept as a definitional floor. New runtime detectors: `strategy/social_evidence.py` (watched real-task completions via the `crew_tasks_remaining` decrement + >=56-tick dwell gate — the strongest cue at -10.8, imposters produced ZERO in 62k rows; chat stances; attributed vote dots) + the MeetingCall-interstitial caller parse (game 4b9297d) -> `reported_bodies`/`button_calls_made` (-1.59). **Crewmate vote: P>=0.9 only, NO clear-leader rule** (the league-data mis-vote engine: 58% of v24's player-votes hit crew; held-out sim: 94% imposter precision at the new bar). Imposter deflection keeps legacy clear-leader logic on the fitted posterior. Legacy hand model = fallback (`CREWBORG_SUSPICION_WEIGHTS=0`). 356 tests. Gate-1 PASS (clean exit, weights verified in-image, 0 errors in 8 player logs). **Submitted to Crewrift league 2026-06-12** (James's explicit conditional go-ahead). **A/B vs v24 DONE** (crew config `xreq_94491df3`/`xreq_3955617c`; 2-imp `xreq_5b674110`/`xreq_e7b4c980`; 40 eps each, pinned roster vs the NEW field — sussybuster-aaln:v1, truecrew:v20/21): **crew win 22%→35%** (+13pp, p=0.22, underpowered) with the mechanism overwhelming — votes-at-crew **0.88→0.05/g**, ejection complicity 0.23→0.05, **own ejection rate 52%→2%** (21/40 vs 1/40 — the evolved field was voting accuse-heavy v24 OUT; restraint fixed it), team crew-ejections 30→6, tasks-done-alive 95%→100% and faster (median tick 5323→4724). **Imposter regression scan: none** — kills 0.89→1.30 (p=0.01, up), win 42%→30% (p=0.33 noise), ejected 11%→7%. 14/160 episodes were instant-GameOver degenerates (new ops signature, see tentative lesson) — excluded. Verdict: the fitted model + vote restraint does exactly what the league data predicted; v25 stays submitted. |
| v24 | `b725a6e1-2704-4258-b77e-d3ecabcd1d5f` | 2026-06-11Z | `jboggs/crewborg-imposter-meetings` @ `2199e4c`; image via `tools/build_player.sh crewborg` | **v22 env, NO `CREWBORG_BE_DUMB`** (`CREWBORG_METRICS=1`, `CREWBORG_TRACE_GROUPS=voting,action,decision`, `CREWBORG_TRACE_DECISION_FIELDS=…`, `CREWBORG_CHAT_NLP=1`) | **Imposter "get the kill in sooner" (real source change vs v22).** After the v23 BE_DUMB ceiling arm proved pure aggression a bad trade (2.25→2.47 kills for 14%→40% ejection; cap is cooldown+isolation, not blending time), three targeted changes (per James): **(1)** `SEARCH_LEAD_TICKS` 100→**250** — start shadowing an isolated victim at the cooldown's midpoint, not its last fifth; **(2)** Pretend `DO_TASK` holds a fake task **only while a crewmate is visible** (`has_visible_victim`) — an unwatched fake task fools no one and burns cooldown; **(3)** the hold **stops the instant** the last crewmate leaves view, re-dispatching toward crew/victims. Rationale: a sloppy partner's kill→report resets *our* kill cooldown, so banking our own kill ASAP is the only lever on our side. Keeps Evade + a Pretend window (unlike BE_DUMB). 334 tests (+2 new), ruff clean, design.md synced. Image verified `SEARCH_LEAD_TICKS=250` + audience gate present. **Evals:** 1-imp A/B (`xreq_7ae00985` v22 / `xreq_c3f4b252` v24) was **inconclusive by design** — 1-imp has no partner, so it can't test the partner-report-CD-reset mechanism (James's rule now: **always 2-imp evals**). **2-imp A/B** (crewborg slot 0 + partner slot 7, 6 crew, 30 eps; v22 `xreq_dff96e86` / v24 `xreq_a62759e9`, game v0.1.52): v24 **1.90 kills/g vs v22 1.67** (+14%; ≥3-kill games 7%→21%), reversing the 1-imp sign — consistent with the hypothesis, but underpowered (t≈1.35, p≈0.18). Mode shift confirmed (pretend 66%→42%, search 23%→47%). **Submitted to Crewrift league 2026-06-11 as `sub_e6969016-cf97-4549-bf9f-174fb29b805e`** (Gate 2, James's go-ahead — provisional, pending the large A/B; supersedes v22's `sub_9a4b4fa9`). **Large A/B was CONFOUNDED** (`top_n` seated a different slot-7 partner per arm → the +23% win was a partner artifact). **CONTROLLED re-run** (explicit fixed roster, partner=slava2 in both arms, only slot 0 differs; 100 eps/arm, v22 `xreq_1c7f6bdf` / v24 `xreq_57de3453`): **kills +0.21/g (1.37→1.58, +15%), t=2.21, p=0.027 SIGNIFICANT** (≥2-kill 41%→55%); the same ~+0.2 held across all three batches despite different partners → robust policy effect. **Win +6% (47%→53%), p=0.40 NOT significant** — the kill gain doesn't translate to a detectable win improvement; no ejection cost (v24 wins slightly more, keeps Evade). Verdict: v24 = self-vote fix + a real, confirmed kill bump at zero ejection cost → strictly better than v22, keep submitted; but kill→win link is weak, so further kill tuning is diminishing returns for *winning*. |
| v23 | `2ba6a477-cb67-454d-a59c-c7cacc6d6fad` | 2026-06-11Z | **same image as v22** (`players-crewborg:dev`, code `fd04dd3`; verified `self_color`+BE_DUMB logic present, amd64) — re-uploaded with one extra env | v22 env **plus `CREWBORG_BE_DUMB=1`** (`CREWBORG_METRICS=1`, `CREWBORG_TRACE_GROUPS=voting,action,decision`, `CREWBORG_TRACE_DECISION_FIELDS=…`, `CREWBORG_CHAT_NLP=1`) | **Imposter kill-ceiling experiment arm (no source change).** `CREWBORG_BE_DUMB=1` strips Pretend/Evade/Report from the imposter priority — only Search/Hunt: hunt when kill-ready with a visible victim, else search (`rule_based._select_imposter`). Motivation: v22 replay+trace analysis of 5 imposter games found crewborg **survives every game / never ejected** but under-kills because it spends **54–74% of ticks in `pretend`** and enters `hunt` only **0.1–2.9%** — its kill *conversion is ~100%* (`kill_attempted==kills`), so the bottleneck is too few **attempts**, caused by `SEARCH_LEAD_TICKS=100` positioning it only in the last fifth of the 500-tick cooldown. This arm measures the aggression ceiling + ejection cost before tuning `SEARCH_LEAD_TICKS`/Search-stalk. A/B (both imposter-pinned, 1-imp, vs Competition top-7, 30 eps): baseline v22 `xreq_9274d50f`, BE_DUMB v23 `xreq_93e35801`. **NOT submitted to any league.** |
| v22 | `40e29a8c-fb17-493b-a5d9-6782d5ec4f5c` | 2026-06-11Z | `jboggs/crewborg-imposter-meetings` @ `fd04dd3`; image via `tools/build_player.sh crewborg` | identical env to v21 (`CREWBORG_CHAT_NLP` on, trace `voting,action,decision`) | **Critical bugfix vs v21: never suspect/vote ourselves.** Replay analysis of v20/v21 crew losses found crewborg's own sprite leaking into the roster (it sits at the camera centre) → the `tailing_self` detector logged "self tailing self" every tick → our own colour saturated at p≈0.72 → `top_suspect` returned *self* → we accused + voted to eject **ourselves** ("red sus: they were tailing me"; sim recorded red voting red). Fix: learn `belief.self_color` (camera-centre sprite + voting self-marker) and exclude it from tailing, scoring, `top_suspect`/`active_tail_suspect`, and the ballot (hard guard). 332 tests + 7 regressions. Sanity eval (10 crew games vs top-7) `xreq_1ea0670e-af77-41aa-a6b2-f55baa5c2078` ran **10/10, 0 failures, 0 connect-failures**; replay-expanded two games (crewborg colour `red`) and confirmed it voted skip/yellow/orange — **real other players, never itself** (fix verified). **Image build verified `chat_nlp.state()=='ready'`** (v21 click fix holds). **Submitted to Crewrift league 2026-06-11 as `sub_9a4b4fa9-a0a7-498e-bbb3-4bddfb0e5730`** (Gate 2; supersedes v21's `sub_2c8afd84` — retire that buggy membership). |
| v21 | `52fc8572-52ac-44cc-83a8-0aee978e197e` | 2026-06-11Z | same code as v20 (`jboggs/crewborg-imposter-meetings` @ `e549769`); **local `coworld/Dockerfile` adds `click`** to the spaCy install | identical to v20 (`--run …policy_player`; `CREWBORG_METRICS=1`; `CREWBORG_TRACE_GROUPS=voting,action,decision`; decision fields; `CREWBORG_CHAT_NLP` on) | **Fixes the v20 chat-NLP bug.** v20's eval traces showed `meeting_decision.nlp=failed` on every imposter episode: `import spacy` → `spacy.cli` → `click`, and the slim image lacked `click` (typer landed without it), so the model never loaded and the bandwagon silently degraded to vote-only. Adding `click` fixes it (verified: rebuilt image reaches `chat_nlp.state()=='ready'` through the real background loader). Mixed-role eval (48 eps, `rotate_seats`, random roles vs top-7) `xreq_d69d774e-9cda-4d5b-9059-35dc6cfd1acb` — **48% of those episodes hit platform-wide connect-timeout failures (−100, all players, not crewborg)**, a live-platform issue (also seen in the main league); clean-pool analysis (v20 40/40 + v21 25/48 = 65 eps) confirms crewborg = best crewmate by score / **weakest imposter by kills (1.58, lowest in field)**, win rate saturated (imp ~95–100%, crew ~2–7% — opponent-pool balance). **Submitted to Crewrift league 2026-06-11 as `sub_2c8afd84-2b32-40f0-8f6d-4fe795e5122c`** (Gate 2). |
| v20 | `e6857742-6373-4db8-9329-c5647627be56` | 2026-06-11Z | `player_labs` branch `jboggs/crewborg-imposter-meetings` @ `e549769` (stacked on `jboggs/crewborg-accuse-and-voting`); image via `tools/build_player.sh crewborg` (local coworld `Dockerfile` adds `spacy`+`en_core_web_sm` — the `coworld/` dir is gitignored, so that build edit lives only in the working tree) | `--run python --run -m --run crewborg.coworld.policy_player`; `CREWBORG_METRICS=1`; `CREWBORG_TRACE_GROUPS=voting,action,decision`; `CREWBORG_TRACE_DECISION_FIELDS=phase,role,mode,intent,command,voting,self`; `CREWBORG_CHAT_NLP` default on | **Big jump vs v19 — the whole meeting overhaul.** Carries ground-truth tick + kill-CD 500 (the prior unbuilt "v20" main work), the **voting** change (witnessed catches unified into the event log; `tailing_self` signal; vote on a *clear leading suspect*; `"<color> sus: <reasons>"` accusation chat — PR #1), **Accuse mode** (replaces Flee: an active tail → go slam the emergency button → accuse), and **imposter meetings** (deflect onto crewmates / bandwagon with safe fabrication; spaCy dependency-parse chat NLP behind `CREWBORG_CHAT_NLP`, background-loaded — PR #2). NEW `domain.meeting_decision` trace (path/target/heat/nlp-state). Hosted eval: **14 role-locked rotating bundles, 20 crew + 20 imp vs the live top-7** (imposter role rotated across all opponents); bundle 0 ran 3/3 clean (slots resolved `[crew,imp,imp,crew,…]`, crewborg slot 0). xreqs `crew: 356bc923,25264f00,7df8a35b,88034b0b,3405cd36,4e304622,e9cc966e`; `imp: 1bb8a18e,f6404ac7,9b4fcd32,9d766776,f4701e16,652a06ad,f780c78c`. **Eval findings:** crewborg now votes in both roles (23 crew + 33 imp player-votes, **0 timeouts** — objective met) and the meeting brains fire as designed (crew accuse/skip; imp proactive/bandwagon/skip). **Win rate is matchup-saturated** — *every* player wins ~100% imp / ~0–5% crew, so it can't rank players here; the discriminating signals are crewborg = best-tasking crewmate (score 12.6, tasks 7.6/8) and **under-killing imposter** (1.55 kills vs field ~1.85). **Bug caught by the new tracing:** `meeting_decision.nlp=failed` on every imp episode — spaCy didn't load (missing `click`), so chat NLP degraded to vote-only. Fixed in **v21**. **NOT submitted to any league.** |
| v19 | `358ec5fb-33f4-411e-b745-181ba932293d` | 2026-06-10T20:18Z | `player_labs` branch `jboggs/trace-artifact-outputs` @ `1fe2447`; image via `tools/build_player.sh crewborg` | `--run python --run=-m --run crewborg.coworld.policy_player`; `CREWBORG_METRICS=1`; `CREWBORG_TRACE_GROUPS=voting,action,decision`; `CREWBORG_TRACE_DECISION_FIELDS=phase,role,mode,intent,command,voting,self` | **Offline nav-bake** (vs v18): nav graph + occupancy substrate baked once into vendored `map/croatoan_navbake.pkl.gz` and loaded at runtime (live-build fallback on mask mismatch) — kills the ~13.7s first-tick spawn-freeze. Hosted probe `xreq_94340f39` (3 eps): **tick-1 `bridge.step_ms` 52–72ms vs v18's ~13,700ms (~200×)**, steady-state unchanged; baked mask matched the deployed game (loaded, no fallback). Play byte-identical to live build (verified). **Submitted to Crewrift league** 2026-06-10 as `sub_2d6d92bf-5c43-4727-829b-0a5173030b21` (Gate 2; rationale: same brain as champion v17 minus the ~14s spawn freeze). |
| v18 | `bd309093-e146-48ba-9a09-cc217c7fd383` | 2026-06-10T18:07:40Z | `player_labs` branch `jboggs/trace-artifact-outputs` @ `641d1f8`; image `players-crewborg:dev` via `tools/build_player.sh crewborg` (SDK resolved to players `146905e3`) | `--run python --run=-m --run crewborg.coworld.policy_player`; `CREWBORG_METRICS=1`; `CREWBORG_TRACE_GROUPS=voting,action,decision`; `CREWBORG_TRACE_DECISION_FIELDS=phase,role,mode,intent,command,voting,self` | **Observability-only change vs v17** (no strategy/behavior diff): tracing routed through SDK `TraceOutputs`, default `jsonl@artifact` (player-artifact zip upload; stderr fallback when no URL); NEW bridge latency metrics (`bridge.step_ms`/`loop_gap_ms`/`tick_drift`, tick-tagged) + `voting` section in `decision_snapshot` (cursor_slot/on_skip/candidates/vote_confirmed). Purpose: hosted probe of the artifact pipeline + Python-latency / vote-actuation investigation. Local Gate-1 smoke PASS against Crewrift `0.1.46` (fallback path exercised — local coworld 0.1.20 runner predates the artifact URL). NOT submitted to any league. |
| v17 | `bd97b769-57fc-4279-a0b0-fc628e056a2d` | 2026-06-10T00:26:39Z | `player_labs` worktree based on `02f31a1` with vote-confirmation persistence fix; image `players-crewborg:dev` built via `tools/build_player.sh crewborg` | `--run python --run -m --run crewborg.coworld.policy_player`; `CREWBORG_TRACE_GROUPS=voting,decision`; `CREWBORG_TRACE_DECISION_FIELDS=phase,role,mode,intent,command` | Metrics disabled by omitting `CREWBORG_METRICS`. Local Gate-1 smoke passed against Crewrift `0.1.46`; 10-episode XP request `xreq_59140c47-b54e-451b-aaf9-60e327d77341` completed 10/10 with 0 failures. 100-episode rotated top-7 XP request `xreq_d59e5db8-fb98-4fef-83c3-574013bf1a1b` completed 100/100 with 0 failures; crewborg had 0 eligible missed votes and ranked 2nd by mean score in that batch. Submitted to Crewrift league as `sub_6778090c-e843-4cb1-8bb3-67f7a00853d0`; qualifying membership `lpm_1c79d40c-2383-472b-a311-a44de9263a8c`. |
| v16 | `27d9f9cc-6e38-4b23-9e41-a2da981cd112` | 2026-06-09T23:25:02Z | `player_labs` `02f31a1`; image `players-crewborg:dev` built via `tools/build_player.sh crewborg` | `--run python --run -m --run crewborg.coworld.policy_player`; `CREWBORG_TRACE_GROUPS=voting,action,decision`; `CREWBORG_TRACE_DECISION_FIELDS=phase,role,mode,intent,command`; `CREWBORG_METRICS=1` | Voting-audit upload. Local Gate-1 smoke passed against Crewrift `0.1.46` after adding the required `--run` override. |
