# Pipeline OUTPUTS — generated by the pipeline, NEVER vendored (corpus alone is ~50G).
# A fresh checkout has NONE of these; run the pipeline (README.md A→D) to fill them.
corpus/
expanded/
dataset/
models/
logs/
__pycache__/
*.pyc
